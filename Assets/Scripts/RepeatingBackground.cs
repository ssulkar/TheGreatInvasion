using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RepeatingBackground : MonoBehaviour {
	/*
	public float spawnXPosition, spawnYPosition;
	public float resetXPosition, resetYPosition;
	public float poolSize;

	//public GameObject[] objectsToUse;
	public GameObject prefab;

	private GameObject[] objects;

	// Use this for initialization
	void Start () {
		objects = new GameObject[poolSize];
		for (int i = 0; i < poolSize; i++)
		{
			float randomXAxis = Random.Range (resetXPosition, spawnXPosition);
			float randomYAxis = Random.Range (resetYPosition, spawnYPosition);
			Vector2 randomPosition = new Vector2 (randomXAxis, randomYAxis);
			objects[i] = Instantiate(prefab, randomPosition, Quaternion.identity);
		} 
	}
	
	// Update is called once per frame
	void Update () { 
		
	}

	void resetPosition(){
		
		Vector2 = new Vector2(spawnXPosition, Random.Range(resetYPosition, spawnYPosition));

	}*/
}
